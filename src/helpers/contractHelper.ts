import Networks from "@configs/networks"
import { ethers } from "ethers"

export const getContract = (abi: any[], address: string, signer?: ethers.Signer | ethers.providers.JsonRpcProvider) => {

}

export const getRarityContract = () => {

}

