export type RarityClassType = {
    [key: number]: {
        title: string,
        description: string
    }
}

export type RarityAttributesType = {
    [key: number]: {
        title: string
    }
}