import { MeshBuilder, Scene, TransformNode } from 'babylonjs';


class BaseRPNPC extends TransformNode {

  constructor(name: string, scene: Scene) {
    super(name, scene);

    
  }

}

export default BaseRPNPC